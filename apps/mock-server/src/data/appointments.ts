import { mockAppointments, mockProviders } from "../../../../packages/shared/src/mocks/appointments.ts";

export const appointments = [...mockAppointments];
export const providers = [...mockProviders];

