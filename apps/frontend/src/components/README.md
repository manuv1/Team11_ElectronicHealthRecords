# Components

Suggested component groups:

- `ui` shared primitives
- `patients` patient cards, summaries, and forms
- `appointments` schedule views and booking flows
- `lab-results` result lists and detail panels
- `medications` medication lists and prescribing forms
